/resource/static :此文件夹为默认资源文件夹，可以通过地址栏直接访问js,css等。xxx.html页面也可以直接通过类似“localhost:8080/xxx.html”进行访问。
/resource/templates：此文件夹为thymeleaf 下 默认的视图放置区域。通过路由解析进行访问。
/resource/application.properties :此文件为默认配置文件